import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
  // Next 16: Turbopack is default bundler
  // No additional config needed unless custom overrides required
});
